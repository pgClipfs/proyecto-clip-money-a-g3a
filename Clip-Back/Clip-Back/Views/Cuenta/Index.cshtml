@model IEnumerable<Clip_Back.DAL.Entities.Cuenta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.usuario_cvu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.saldo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nro_cuenta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_Usuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.usuario_cvu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saldo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nro_cuenta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_Usuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_cuenta }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_cuenta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_cuenta })
        </td>
    </tr>
}

</table>
