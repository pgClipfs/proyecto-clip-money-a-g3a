@model IEnumerable<Clip_Back.DAL.Entities.Transferencia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cvu_origen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cvu_destino)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cbu_destino)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.monto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cvu_origen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cvu_destino)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cbu_destino)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.monto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_transferencia }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_transferencia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_transferencia })
        </td>
    </tr>
}

</table>
