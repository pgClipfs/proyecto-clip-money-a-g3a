@model IEnumerable<Clip_Back.DAL.Entities.Moneda>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.valoractual)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.valoractual)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_moneda }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_moneda }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_moneda })
        </td>
    </tr>
}

</table>
